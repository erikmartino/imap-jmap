# Radicale authentication configuration for LDAP
[server]
hosts = 0.0.0.0:5232

[auth]
type = ldap
ldap_url = ldap://ldap:389
ldap_base = ou=users,dc=example,dc=org
ldap_attribute = uid
ldap_bind_dn = cn=admin,dc=example,dc=org
ldap_password = admin
